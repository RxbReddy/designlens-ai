# Project Rules

## RTK - Rust Token Killer

Always prefix shell commands with `rtk` to minimize token consumption and filter output before it reaches the model context.

### Examples:
- `rtk git status`
- `rtk cargo test`
- `rtk ls`
- `rtk grep "pattern"`
- `rtk find`
- `rtk docker ps`
- `rtk gh pr list`
